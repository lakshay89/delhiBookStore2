/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["res.cloudinary.com", "localhost", "picsum.photos", "api.delhibookstore.com", "https://", "http://", "https://api.delhibookstore.com/"],
  },
};

export default nextConfig;
